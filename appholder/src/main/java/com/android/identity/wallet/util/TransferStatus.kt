package com.android.identity.wallet.util

enum class TransferStatus {
    QR_ENGAGEMENT_READY,
    ENGAGEMENT_DETECTED,
    CONNECTING,
    CONNECTED,
    REQUEST,
    REQUEST_SERVED,
    DISCONNECTED,
    ERROR
}